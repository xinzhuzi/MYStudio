// MYStudio-owned integration seam. The upstream vendor snapshot is never imported by the renderer.
export {
  createAitoearnLocalAdapter,
  SelfMediaProviderError,
  type AitoearnLocalPlatformBridge,
  type SelfMediaProviderAdapter,
  type SelfMediaProviderPublishContext,
} from "../../provider-registry";
