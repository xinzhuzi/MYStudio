# MYStudio TTS/STT Backend Dependencies
huggingface_hub>=0.23.0
numpy>=1.24.0
miniaudio>=1.59
transformers>=4.44.0

# ─── Apple Silicon: MLX 推理（TTS + STT） ───
mlx>=0.30.0; sys_platform == "darwin" and platform_machine == "arm64"
mlx-audio==0.4.1; sys_platform == "darwin" and platform_machine == "arm64"

# ─── Windows / Linux: PyTorch 推理（TTS + STT） ───
# GPU 加速需安装 CUDA 版 torch，由 apps/build/setup-win.ps1 处理
torch>=2.5.1; sys_platform != "darwin"
qwen-tts; sys_platform != "darwin"
