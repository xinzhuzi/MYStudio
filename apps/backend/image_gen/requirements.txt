# Qwen-Image-Edit-2511 本地生图依赖(08-28 尖刺实证矩阵)
# 版本坑(全部踩过,勿随意放宽):
# - diffusers 0.40 元数据要 hub>=1.23(<2.0);transformers 4.57 要 hub<1.0 → 4.x 系排除,须 5.9
# - get_cached_repo_tree ImportError = hub 被降到 0.x 的征兆,升回 1.x
# - GGUF 主模型需 gguf + accelerate;TE 单文件直载需 safetensors
torch>=2.5.1
torchvision
diffusers==0.40.0
transformers==5.9.0
huggingface_hub>=1.23,<2.0
accelerate
gguf
safetensors
pillow
# opencv-contrib 含 ximgproc.guidedFilter(uncloth V3 蒙版边缘精修,LayerStyle 同构;勿装无 contrib 变体)
opencv-contrib-python-headless
scipy
# mkl 色彩迁移(uncloth 遍2 后处理 ColorMatch 同构;GPL-3.0 依赖引用,AGPL 仓合规)
color-matcher
