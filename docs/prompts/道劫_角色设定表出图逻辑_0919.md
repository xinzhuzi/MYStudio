# 道劫·角色设定表出图逻辑(2026-09-19 收编定稿)

> **两步链**:角色设定(身份/境界/服饰/气质)→ 主体句(七段公式)→ 立绘(t2i 人物型)→ 设定板(charsheet 链 i2i)。
> 本文=「角色设定表」章的成文真源(速查卡 [66] 摘要版见 K2-文生图-道劫.json);数值单源=`daojie_bases.json`(v3)+`daojie_lora_stack.json`(14 槽预设,含「设定板」档),两处热改即时生效,本文是只读投影。
> 实弹台账:`docs/prompts/道劫_设定表实弹_0919/runs_audit.json`(改造前零回归双跑+改造后 A/B 全参数);图双落 `~/Downloads/daojie_charsheet_0919/` 与仓库同名目录。AI 初评口径,终审权在用户。

## 一、两步链总览

```
角色设定文本(身份/境界/服饰/气质/神色)
  → 主体句(七段公式;握持语义按 hands-pose A 层纪律)
  → [步1] 立绘:K2-文生图-道劫.json(t2i)
          [80]=人物 · [50]=主体句全文 · seed=42 · 3:4 4.2MP(1816×2424)· 速度档 4 步
  → [步2] 设定板:K2-角色设定-道劫.json(charsheet i2i)
          [72] 参考图=步1 立绘 · [90] preset=设定板档(turbo 急停,§三)· [304] 底座(默认型见 §三)
          · 10 步 LCM cfg1 · 2568×1712(2K 档,09-20 落账)· seed=2027
```

- 步1 复用道劫 t2i 主链(零新建);步2 = 30 节点 charsheet 流(09-19 收编改造版)。
- 两步的「同角色」锚 = **同一主体句全文 + 种子区间恒定**(见 §四)。

## 二、步1 立绘(t2i 人物型)

- 入口:`1_图片/K2图像/1_文生图/K2-文生图-道劫.json`;[80] 底座选「人物」(画幅/LoRA 组自动跟随);[50] 只写主体句,风格 DNA 底座自动携带。
- LoRA 组([90] preset=跟随底座型·人物):加速×1+服从度×0.01+细节×1+亚洲面孔×0.4+鎏金×0.3。
- 种子口径:配方验证一律 seed=42;同角色复跑换图保持同种子。
- 立绘即步2 的唯一身份参考图(charsheet 官方语义:Image 1 is the sole identity and design reference)。

## 三、步2 设定板(charsheet 链 i2i)

- 入口:`1_图片/K2图像/2_图生图/K2-角色设定-道劫.json`(30 节点;原 8 件链整段存同目录 `-专家模式.json` 副本可切回)。

### 模型与 LoRA(链序铁律)

```
[55] krea2_turbo → [90] MyDaojieLoraStack(preset=设定板)→ [164] CharacterSheet LoRA ×1
                 → [131] Reroute → [120] Krea2EditModelPatch → [53] KSampler
```

- **[164] 恒挂且必先于 ModelPatch**(官方挂法,顺序不可动):栈出→[164]→补丁;装机位=引擎 `loras` 根 `DynamicCharacterSheet_krea2_v1.safetensors`(上游 HF Alissonerdx/CharacterSheet)。
- **[90] preset=「设定板」档 + turbo 急停**(daojie_lora_stack.json 第 10 档,=人物组逐槽逐权重复制;急停=栈节点既有「真关+applied 披露」语义):生效组=服从度×0.01+细节×1+亚洲面孔×0.4+鎏金×0.3;**turbo 开=文字乱码放大器**(09-20 单变量定谳,见 §八),要加速先接受乱码代价;钉死不随底座型漂移(防误选场景型掉 asianmix/liujin 点墨系);生效清单=[306] easy showAnything。
- **[304] MyDaojieBase**(设定板步底座,默认「人物」;09-20 对拍定谳:人物 8.5 vs 高清人脸 8.0,AI 初评待用户终审):
  - 负向出 = [64] 用户负向 ∩ 该型负面基线(顶层逗号 token 去重合并,节点装配)→ [85] 同图接地;LCM cfg1 档数学不生效,换 Raw 底模 40 步 cfg3-4 才生效([64] 题注在档)。
  - 正向出 = 底座正向锚,经 **[305] StringConcatenate 殿后拼接**:[163] VLM caption(前)+[304].positive(后,`\n` 分隔)→ [119].prompt。锚殿后=结构化模板完整性优先(格数漂移是已知弱项,锚不插队头)。
- 分辨率:**2K 档恒 2568×1712([156],09-20 用户裁定补课+实弹落账)**:3:2(模板 FIXED LANDSCAPE 锁横幅)× 4.2MP,与 t2i 人物档同 MP 口径;实弹 1045s/张(vs 1536 档 292s,像素 2.8×耗时 3.6×);不接 [61] 联动。同种子换分辨率构图必漂(09-18 在案),2K 与 1536 产物非同构图放大关系。
- **[307] MyCharsheetLabels(汉字标注,09-20 案一落地;三轮=四格条带默认)**:[54] VAEDecode→[307]→[29] SaveImage。**layout=四格条带**(4-View 四格版式配套):每格底部纸色标签带写 grid_labels(默认 正面全身/侧面全身/背面全身/面部特写)+左上角色小卡(姓名大字+精简字段+小朱印),文字全落格间/条带,生成内容零接触(压人问题根除);layout=左栏竖排=旧 Dynamic 七区版式配套(保留可选)。**字体=苹方 PingFang SC 默认**(大陆规范简体字形;宋体 Songti 刻本旧字形被用户判「不是简体中文」09-20 二轮定谳,降为可选;黑体 STHeiti→Hiragino 回退;苹方住系统 AssetsV2 资产目录,glob 现探自愈哈希漂移)。角色名/字段/格标签在 [307] widget 上改,批量=改节点参数。
- **四视图终态(09-20 五轮落账;QuadView 原厂件)**:[164]→`QuadView_krea2_v1.safetensors`(Alissonerdx 原厂 914MB,用户荐件;krea2_turbo_bf16 底模四格触发实证,九型栈不干扰);[53]=**euler(采样器)/simple×10 步**;[120] ref_boost=**1.0**(6.0=参考锁死档,压四格布局——G 系实弹定谳);[119]/[85] grounding_px=**0**(native);提示词源=[308]「四视图·QuadView 触发词」(官方单句,主文件经 [305] 与 [304] 底座锚拼接,专家模式直连 [119]);VLM 三件 mute;[307] layout=四格条带(标签带**自适应对齐**生成内容列+等分兜底+IS_CHANGED 穿透缓存)。参照件死亡档案:civitai Krea2_Character_Design_4-View_V1(2937321)五发零触发已删除(runs_audit post-mod-4view-diag disposal),勿再装入除非 convrot 底模到位重验。2K 实弹 970s;同链路此前『四视图 LoRA 在 bf16 全不行』的推断被 QuadView 实弹推翻——**未实测的件不下结论**。

### 提示词臂

- **VLM 三件([184] 指令模板/[162] TextGenerate/[163] 预览)已激活**(v3 实证):读 [72] 参考图自动重写身份锁,脸部更贴立绘,换角色无需手改锁。
- **VLM 块勿 mute**:mute 后 [305] 只剩底座锚,官方布局模板丢失(设定板会垮)。要停用 VLM:摘 [305]→[119] 连线,[119] 回退内嵌官方模板+道劫锚(题注在档)。
- 负向唯一编辑面=[64](经 [304] 合并)。

### 采样拨盘([302] 卡口径)

- 步数:8=构图/指令服从、12=脸部细节、10=均衡(现值);ref_boost=[120] 前两数(现 1.0 经典档,拉 ~4.0 显著增相似度,>10 破坏编辑);denoise=1.0(参考条件生成,空 latent 起步)。

## 四、主体句模板与同脸锚法

### 七段公式([50] 主体句模板,段序勿倒)

```
一位{性别}修士，{年龄段与境界}，气质{清冷出尘/剑意凌厉/温润如玉}，肤色温润透亮，五官{清隽/英朗/温润}；{发色,默认墨黑}长发{长度}垂落，发丝逐层分明；身着素色道袍{长裙/长衫}，{米白/淡青}纯色，{素布/云锦薄染}质感，衣纹线条流畅；{动作到动词精度:手持何物/拢袖/按剑,身体与视线朝向}，神色{沉静/锐利/含情}；{构图位置,如:立于画面右侧三分之一处,面朝左方}；背景{纯色浅净/淡墨远山}。
```

- 角色设定 → 主体句映射:身份境界→段2、气质五官→段3-4、服饰→段5、动作神色→段6(动作写到动词精度,占位元语言不入正文);服饰朝代口径(汉唐宋明历代)同画面统一,由主体句承载。
- 示例(九型示例句,同脸锚基准):`docs/prompts/道劫_九型主体句示例_0919.md` §1。

### 同脸锚法(同角色跨图同脸)

1. **主体句全文恒定**:立绘步与设定板步用同一主体句全文(步2 的身份一致性另有参考图臂+VLM 身份锁双保险);跨图复用(表情差分/三视图)同此。
2. **种子区间恒定**:立绘步 seed=42(t2i 配方口径)、设定板步 seed=2027(charsheet 现值);同角色复跑不换种子,换种子=换脸风险。
3. 换角色=换主体句全文+换参考图([72]);同角色只调画风件时主体句一字不动。

### 握持纪律(引自 09-17-daojie-k2-hands-pose A 层,不重复建设)

- 主体句动作段优先「拢袖/负手/持物遮掩/半身取景」减少手部暴露(不暴露=不出错);
- 需要露手=写清**手在做什么、握什么、指朝哪**(动词精度);设定板 ACTION POSES 格是露手高发区,身份锁模板未含手部约束时按本纪律写主体句。

## 五、wf2api 三坑自查清单(09-18 在档;09-19 改造后逐条复验)

| # | 坑 | 机理 | 对策(本流实证) |
|---|---|---|---|
| ① | 旁路链重穿 | 被旁路节点(mode=4)不入 API 图,其出线须回追入槽源穿透(Reroute 同理,前端虚拟节点 object_info 无注册) | `daojie_charsheet_logic_0919.py::_resolve_src_sheet`(穿透深度上限 16,照 lf 纪律) |
| ② | image_b 槽 | 未连线的可选 b 槽([119]/[85].image_b、[120].source_latent_b/source_image_b)若按位置序回退,prompt 文本会错喂进 b 槽 | 转换器 widget 序按 object_info 重建、仅 widget 型占位;连线型可选槽一律不进 API 图(p2_duipai pop 先例+本流全量核) |
| ③ | sampling_mode.* 前缀 | [162] TextGenerate 的 DynamicCombo:API 图须给**裸 `sampling_mode` 键=选项 key**(如 "on")+子参**前缀扁平键**(`sampling_mode.temperature` 等);给嵌套 dict 或全前缀键都会 execute 缺参(comfy_api/_io.py DynamicCombo._expand_schema_for_dynamic) | `ui_to_api_sheet` 对 162 整节点独建(09-19 双实证:全前缀键→missing argument;裸键+前缀子参→通过) |

## 六、常见翻车与缓解

- **格数漂移**(已知弱项,09-19 在档):设定板固定七区格式偶有格数/排布漂移;缓解=低 denoise(design 口径)+锚殿后不插队头+身份锁模板 VERBATIM 段保持完整。
- **turbo 勿开**(09-20 定谳):[90] 加速槽在 10步 LCM 本流=文字乱码放大器,默认已急停;开它=左栏元数据乱码(完成度 4/10 重罚面)。
- **画面内汉字不可行**(09-20 CN 对照实弹定谳):官方模板 VISIBLE TEXT 段锁死「All labels must be English only」;单变量把该段与左栏列要求改为简体中文后,K2 左栏画出的是**似字非字的假字**(笔画堆,拼不出可读汉字)——底模文字渲染域=拉丁字母,与 LoRA 无关(turbo 已急停)。**用户裁定案一已落地(09-20)**:模板改无文字版+[307] MyCharsheetLabels 程序叠加真汉字(见 §三),画面文字=100% 准确汉字;备选案(未走)=换 Qwen-Image-Edit 2509 底模(九型 LoRA 全 K2 训,换底模=画风重调)。
- 高清人脸型勿选作默认(09-20 对拍):「面部约占六成」锚偏置诱发动作排/细节区冗余人脸(构图 8.0<8.5);单脸精修走 K2-人脸精修-道劫 辅路。
- VLM 停用姿势错误(mute 三件)=布局模板丢失:按 §三 摘线回退,勿 mute。
- [90] 切专家档忘互斥纪律:画风件同开 ≤1(+鎏金半件),三画风同开=崩源。
- 立绘步换种子=同脸锚断裂:同角色恒 seed 42/2027 区间。

## 七、与九型地基的接线关系

- 设定板档=第 10 预设(daojie_lora_stack.json),=人物组复制;人物/高清人脸两型的 LoRA 组与画幅同构,故 [304] 型选只动正向锚与负面基线,不动 LoRA 组与分辨率。
- 两步链在九型矩阵中的位置:步1=「人物」行;步2=独立 i2i 流(不在九型 t2i 矩阵内),其底座/栈/负向全部吃九型单源。
- 后续入口:表情差分/三视图自动拼板流水线归 C2 路线表(本任务不建)。

## 八、实弹验收(09-20 执行;AI 初评待用户终审)

- **改造前零回归双跑**:同 payload(参考图=步1 立绘/seed 2027)双发,sha256 全同(`24e6c7dd…`);第 2 发 4.0s=引擎输出缓存命中(同 prompt 哈希),如实记:证明同 payload 稳定,冷跑确定性未另证(VLM 采样组 seed=0 固定,09-18 v3 复跑同口径)。
- **改造后 A/B(turbo 开)**:[304] 人物 vs 高清人脸,applied=设定板(5/14) 逐字一致;四维 AI 初评 A 6.4 / B 6.5,**低于 8.5 基线**——拉分维=完成度(左栏文字乱码,读板器 4/10 重罚)+构图(B 主视图与 FRONT 重复)。
- **归因单变量隔离(A2/B2,turbo 急停)**:applied=设定板(4/14)+披露「开关急停:加速·4步蒸馏」;**A2(人物) 8/9/9/8=8.5 与改造前基线同分(同读板器同题板),B2(高清人脸) 8.0**。定谳:
  1. **turbo×1 在 10步 LCM 设定板流=文字乱码放大器**(A vs A2、B vs B2 单变量对照,乱码随 turbo 开关出现/消失);
  2. **型选=人物**(A2 8.5 > B2 8.0,差在构图维——高清人脸锚「面部约占六成」偏置诱发动作排/细节区冗余人脸,B/B2 两发一致复现)。
- **终态固化(=A2 配置)**:工作流默认=[90] 设定板档+turbo 急停、[304]=人物;8.5 基线同口径持平 ✓。
- **2K 补课实弹(09-20,用户裁定 2K)**:[156] override 2568×1712(A2 终态其余恒定/seed2027):成功出图、实际像素=2568×1712 ✓、布局七区完好;wall=1045.4s、6.7MB;随后 [156] 工作流默认值落账 1536×1024→2568×1712,lint 绿。构图漂移如实记:非 1536 产物放大关系。
- **CN 标注对照实弹(09-20,「必须英文?」定谳)**:[184] 指令模板单变量改(VISIBLE TEXT 段+左栏列 English→Simplified Chinese),其余 A2 终态恒定;wall=344.8s;读图定谳=左栏假字(非可读汉字)→ 底模能力边界,结论见 §六汉字条。
- **案一终态落地实弹(09-20)**:模板无文字版+[307] 汉字叠加+2K,零 override 文件本体全链;wall=1042.1s;读图终验=左栏「青珣」姓名+五行身份字段+朱印全部为清晰可读真汉字,画面其余区块无模型残留文字;pytest 96 绿+lint 双绿+引擎 object_info 注册复验过。
- 全卷图:`~/Downloads/daojie_charsheet_0919/`(step1 立绘/pre_mod_dup1/A/B/A2/B2/2K/CN标注)与仓库 `docs/prompts/道劫_设定表实弹_0919/`;台账 `runs_audit.json`(charshot/pre-mod/post-mod/post-mod-attribution/post-mod-2k/post-mod-cn 六段全参数+sha256)。
- 已知口径注:09-18 8.5 基线样图未能在盘定位(引擎 output 只剩本轮三张),同图对拍不可行;本论以「同读板器+同从严题板下,改造前现文件卷=8.5 恰与 09-18 基线分对齐」为口径桥,如实记。
