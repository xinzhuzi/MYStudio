# 排查提示词:Krea2_无衣物_快「节点未使用」疑云(自包含)

> **2026-09-06 历史排查记录**：以下节点编号、文件名、Desktop 端口与重启/历史清理步骤仅对应当时环境，2026-09-20 未重新执行。当前引擎端口和路径从 manifest 获取，排障入口见 [ComfyUI 引擎指南](../settings/COMFYUI_ENGINE_GUIDE.md)；不要直接运行下方旧环境命令。

> 用途:粘贴到另一个会话执行。本文件自带全部背景、已排查事实、步骤与协议,不依赖其他会话记忆。
> 环境:Mac M4 Max,ComfyUI Desktop,后端端口 **17598**(非 8188,先 `lsof -nP -iTCP:17598 -sTCP:LISTEN` 确认活着)。

## 一、背景

工作流 `user/default/workflows/K2图像/改图/Krea2_无衣物_快.json`(krea2edit 指令编辑出图线,18 节点):
载图→缩放1MP→(接地编码×2 + ModelPatch + VAEEncode参考latent)→采样(10步/CFG1/denoise1.0/空latent)→解码→保存。
所有节点标题已带 `[编号]` 前缀。用户报告:「好像有节点没有使用」(截图未逐节点确认)。

## 二、文件层已排查(2026-09-06,结论:无实质未使用)

- 旁路/静音节点:**0 个**(所有 mode=0)
- 输出悬空:仅 `[16]GetImageSize+ 的 count` 与 `[4]SaveImage 的 images`(终节点/附带输出,正常)
- 未接输入口:`[13]/[17] 的 image_b`、`[14] 的 source_latent_b/ref_boost_mask/source_image_b` —— 全是**可选双图/增强口**,单图模式不接是对的
- `[8]VAEEncode` 的 latent 接 `[14]Krea2EditModelPatch.source_latent`(krea2edit 参考机制),**在用**

## 三、待排查的三个假设(按可能性排序)

1. **执行缓存错觉(最可能)**:ComfyUI 重跑时输入没变的节点被 cache 跳过,进度条和日志里不出现 → 视觉上"没使用"。
   验证法:API 跑一次,读 `status.messages` 里的 `execution_cached` 数组=本轮被跳过的节点;换 seed 再跑,该数组应显著缩短。
2. **前端旧缓存**:用户看的是未刷新的旧标签页(文件近期改过:节点标题加编号)。
   验证法:侧边栏刷新→重新打开,核对标题是否带 `[编号]`(带=新版)。
3. **截图里的节点属于画布外的装饰件**:`[22] 流程标签(Label)`、`[42] 使用说明(MarkdownNote)` 本来就不参与执行,视觉上像"没用"。

## 四、执行步骤

1. `curl -s http://127.0.0.1:17598/queue` 确认队列空(有任务先等/报告)。
2. 提交快流等价 API prompt(或让用户 UI 点 Queue),拿 prompt_id:
   - 节点参照:7=LoadImage, 5=缩放, 12=KSampler(seed 换新值), 4=SaveImage,其余按文件连线构造;
   - 简易法:让用户 UI 里换 seed 点 Queue,你只负责读 history。
3. 读 `GET /history/{prompt_id}`:
   - `status.messages` 里 `execution_cached` 的 `nodes` 列表 → 这些是"看似没用"的缓存跳过节点;
   - 无 `execution_error` = 全链健康。
4. 对比:换 seed 再跑一次,`execution_cached` 应只剩 loaders 类节点 → 证明上一轮的"未使用"是缓存。
5. **队列卫生(必须)**:每条测试读完结果立即 `POST /history` body `{"delete":["<prompt_id>"]}` 自删(DELETE 方法 405,只认 POST)。

## 五、判定与输出

- 若 execution_cached 含 [12]采样/[11]解码等主链节点 → 输入未变被缓存,正常,「未使用」是错觉;
- 若出现 execution_error → 抓 node_id/exception_message/traceback 定位;
- 若 UI 有紫色节点(bypass)而文件层 mode=0 → 前端缓存,刷新重开即消。

## 六、本机协议(别踩)

- 端口 17598;`DELETE /history` 恒 405,清理只认 POST;
- 改完 workflow 文件交付必带「侧边栏刷新→重新打开」;用户存着旧标签页时保存会反向覆盖磁盘;
- 重启=用户门禁(SIGTERM 后安静等 Desktop 自愈 30s~10min,别点 UI 别抢 spawn);
- 宣称纪律:execution_cached/history 里有证据才算实测,没有就标推测。
